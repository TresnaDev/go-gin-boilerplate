meta {
  name: logout
  type: http
  seq: 6
}

post {
  url: {{auth_url}}/logout
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
